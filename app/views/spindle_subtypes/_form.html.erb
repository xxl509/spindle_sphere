<%= form_with(model: spindle_subtype, local: true) do |form| %>
  <% if spindle_subtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spindle_subtype.errors.count, "error") %> prohibited this spindle_subtype from being saved:</h2>

      <ul>
      <% spindle_subtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cluster_id %>
    <%= form.text_field :cluster_id, id: :spindle_subtype_cluster_id %>
  </div>

  <div class="field">
    <%= form.label :cluster_no %>
    <%= form.text_field :cluster_no, id: :spindle_subtype_cluster_no %>
  </div>

  <div class="field">
    <%= form.label :count %>
    <%= form.text_field :count, id: :spindle_subtype_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
