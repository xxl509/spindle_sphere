<%= form_with(model: spindle, local: true) do |form| %>
  <% if spindle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spindle.errors.count, "error") %> prohibited this spindle from being saved:</h2>

      <ul>
      <% spindle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.text_field :patient_id, id: :spindle_patient_id %>
  </div>

  <div class="field">
    <%= form.label :spindle_id %>
    <%= form.text_field :spindle_id, id: :spindle_spindle_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
